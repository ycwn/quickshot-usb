

cc = sdcc
cp = sdcc -E
as = gpasm
ap = sdcc -E
lb = gplib
ld = gplink
pg = pk2cmd

cflags  += -I$(CURDIR) -I$(core_dir)
asflags +=
ldflags += $(patsubst %,-I%,$(shell sdcc $(cflags) --print-search-dirs | awk 'dir; /:/{dir=0} /libdir:/{dir=1}'))
pgflags += -m -y
libs    +=

